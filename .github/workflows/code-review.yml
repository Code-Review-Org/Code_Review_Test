name: Call Gemini Code Review
on:
  pull_request:
    types: [opened, synchronize]
permissions:
  pull-requests: write
jobs:
  call-review:
    uses: Code-Review-Org/central-code-review-Bot/.github/workflows/code-review-reusable.yml@main
